doctype html
html
  head
    title 信令服务器控制台
    script(src='/socket.io/socket.io.js')

  body

    div(class="console")

      div(class="clinets")
        h2 客户端列表

        for user in clients
          p= '[ip]:' + user.ip + ' [id]:' + user.socketId
    

    script.
      var socket = io.connect('http://localhost:9001')

      setTimeout(function() {
        socket.on('message', function (data) {
          window.location.reload()
        })
      }, 1000)
      

    
      